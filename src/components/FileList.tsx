const FileList = () => {
    //Call backend to get list of file names and hashes
    //use ipfs hook and file hashes to get all files
    //Create list of files that show authors name, file, and asteroid 
    //Bounty proposal button
    return (<div>File List</div>)
};

export default FileList;